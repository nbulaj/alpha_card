# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'rubocop', '~> 0.71'
gem 'sorbet', group: :development, platform: %i[ruby]

group :test do
  gem 'coveralls', require: false
  gem 'rack-test', require: false
end
